eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@mongo-atlas-cluster.r1se12i.mongodb.net/orderdb
      host: localhost
      port: 27017
      database: orderdb
      repositories:
        enabled: true
